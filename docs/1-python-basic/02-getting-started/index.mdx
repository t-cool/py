---
sidebar_position: 2
---

import ViewSource from "@site/src/components/ViewSource";
import Answer from "@site/src/components/Answer";

# はじめての <ruby>Python<rt>パイソン</rt></ruby>

さあ、いよいよ<ruby>自分<rt>じぶん</rt></ruby>でプログラムを書いてみましょう。

## Hello World（ハロー・ワールド）

プログラミングを始める人が、最初にやる「<ruby>決<rt>き</rt></ruby>まり」のようなものがあります。
それは、<ruby>画面<rt>がめん</rt></ruby>に `Hello World!`（こんにちは、世界。）と<ruby>表示<rt>ひょうじ</rt></ruby>させることです。

下のコードを見てみてください。これが、画面に文字を出すためのプログラムです。
右側のエディタ（プログラムを書く場所）で<ruby>実行<rt>じっこう</rt></ruby>ボタンを<ruby>押<rt>お</rt></ruby>してみましょう。

<ViewSource path="/getting-started/hello_world.ipynb" />

`Hello World!` と出てきましたか？おめでとうございます。これで皆さんもプログラマーの<ruby>仲間<rt>なかま</rt></ruby>入りですね。

ここで使った `print`（プリント）は、コンピュータに「この文字を画面に出してね。」とお願いする<ruby>命令<rt>めいれい</rt></ruby>です。
`"`（ダブルクォーテーション）という<ruby>記号<rt>きごう</rt></ruby>で<ruby>囲<rt>かこ</rt></ruby>った文字が、画面に出てきます。

### <ruby>練習問題<rt>れんしゅうもんだい</rt></ruby>

さっきのプログラムを少し書きかえて、`Hello from Python!` と表示されるようにしてみましょう。

<Answer>

`"` と `"` の<ruby>間<rt>あいだ</rt></ruby>にある文字を、`Hello World!` から `Hello from Python!` に<ruby>変<rt>か</rt></ruby>えるだけで<ruby>大丈夫<rt>だいじょうぶ</rt></ruby>ですよ。

  <ViewSource path="/getting-started/hello_from_python.ipynb" />
</Answer>

## コメント（自分用のメモ）

プログラムの中に `#` を書くと、そのあとの文字はコンピュータが<ruby>無視<rt>むし</rt></ruby>してくれます。
これを「コメント」と<ruby>呼<rt>よ</rt></ruby>びます。

プログラムの<ruby>説明<rt>せつめい</rt></ruby>を自分あてに書いたり、<ruby>一時的<rt>いちじてき</rt></ruby>にそのコードを動かしたくないときに使ったりします。

<ViewSource path="/getting-started/comment.ipynb" />

<ViewSource path="/getting-started/comment2.ipynb" />
